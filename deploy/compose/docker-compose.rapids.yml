version: '3.8'

services:
  rapids-forecasting:
    build:
      context: .
      dockerfile: Dockerfile.rapids
    container_name: rapids-forecasting
    runtime: nvidia
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=compute,utility
      - CUDA_VISIBLE_DEVICES=0
    volumes:
      - ./scripts:/app/scripts
      - ./data:/app/data
      - ./phase1_phase2_forecasts.json:/app/phase1_phase2_forecasts.json
      - ./rapids_gpu_forecasts.json:/app/rapids_gpu_forecasts.json
    networks:
      - warehouse-network
    depends_on:
      - postgres
    command: python scripts/rapids_gpu_forecasting.py

  postgres:
    image: timescale/timescaledb:latest-pg15
    container_name: warehouse-postgres
    environment:
      POSTGRES_DB: warehouse
      POSTGRES_USER: warehouse
      POSTGRES_PASSWORD: warehousepw
    ports:
      - "5435:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./data/postgres:/docker-entrypoint-initdb.d
    networks:
      - warehouse-network

volumes:
  postgres_data:

networks:
  warehouse-network:
    driver: bridge